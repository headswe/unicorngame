name: Deploy to GitHub Pages

on:
  push:
    branches: [claude/unicorn-game-build-88xv4l]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Never let two deploys race; queue them instead of cancelling, so the last
# push always ends up being what is live.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      # Playwright is only used for local screenshots — CI has no need to pull
      # down a browser to build the game.
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: '1'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - run: npm ci

      # Runs the typecheck as well, so a broken build never reaches Pages.
      - run: npm run build
        env:
          # Where the shared meadow lives. Baked in at build time, and public by
          # nature — it is the address a child's browser dials, so it ends up in
          # the bundle whatever we do. Set the `VITE_ANGEN_RELAY` repository
          # variable to point somewhere else without touching this file; leave
          # it empty to ship a single-player build.
          VITE_ANGEN_RELAY: ${{ vars.VITE_ANGEN_RELAY || 'wss://app-unicorngame-prod-001-ckhwanafbsetexfh.swedencentral-01.azurewebsites.net/angen' }}

      # Pages is enabled in the repository settings; the workflow token is not
      # allowed to create the site itself, so this only reads the config.
      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
